import { style } from '@vanilla-extract/css';

export const kakaoLoginButton = style({
  backgroundColor: '#FEE500',
  borderRadius: '4px',
  height: '48px',
  width: '100%',
  textAlign: 'center'
});
