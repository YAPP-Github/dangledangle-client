import { style } from '@vanilla-extract/css';

export const logoWrapper = style({
  margin: '80px auto'
});

export const buttonWrapper = style({
  marginTop: '40px'
});

export const findTextWrapper = style({
  display: 'flex',
  justifyContent: 'flex-end'
});

export const btnTextWrapper = style({
  margin: '16px 0 0 auto',
  cursor: 'pointer'
});

export const registerTextWrapper = style({
  display: 'flex',
  columnGap: '10px',
  justifyContent: 'center',
  marginTop: '34px'
});
